package test

object Test {
  def main(args: Array[String]): Unit = {
    print("开了一个新分支，完成了一个新需求...")
    print("完成了该分支所有的需求，可以合并到master分支了")
    print("发布v1.0.0版本，并打包到远程仓库")
    print("正在开发v2.0.0..........")
  }
}
